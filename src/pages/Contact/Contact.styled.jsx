import styled from "styled-components";

export const Contact = styled.div.attrs({ className: "contact-content outlet-content" })`
  .contact-content {
    margin-top: 10px;
    p {
      color: var(--color-denary);
    }
  }
`;
